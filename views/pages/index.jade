extends ../layout

block content
    .container
        .row
            each item in movies
                .col-6.col-md-4.col-lg-3
                    .card
                        a(href="/movie/#{item._id}")
                            img(class="card-img-top", src="#{item.poster}", alt="#{item.title}")
                        .card-body
                            h5(class="card-title") #{item.title}
                            a.btn.btn-primary(class="card-text", href="/movie/#{item._id}", role="button") 观看预告片